name: Deploy to DEV with Docker Swarm

on:
  push:
    branches: [development]
  workflow_dispatch:
    inputs:
      image_tag:
        description: 'Docker image tag to deploy'
        required: true
        default: 'development'

# Environment variables
defaults:
  run:
    shell: bash

env:
  IMAGE_NAME: academianovit
  DEFAULT_TAG: development
  SSH_OPTS: "-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ConnectTimeout=30"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set image tag
        id: set_tag
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "tag=${{ github.event.inputs.image_tag }}" >> $GITHUB_OUTPUT
          else
            echo "tag=${{ env.DEFAULT_TAG }}" >> $GITHUB_OUTPUT
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          install: true

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          tags: ${{ env.IMAGE_NAME }}:${{ steps.set_tag.outputs.tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Setup SSH connection
        run: |
          set -ex  # Enable debug mode and exit on error
          
          echo "🚀 Setting up SSH connection..."
          echo "🔍 Debug Info:"
          echo "- SSH_HOST: ${{ secrets.SSH_HOST }}"
          echo "- SSH_PORT: ${{ secrets.SSH_PORT }}"
          echo "- SSH_USER: ${{ secrets.SSH_USER }}"
          
          # Create .ssh directory
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          
          # Save private key
          echo "🔑 Saving SSH private key..."
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          
          # Verify private key was saved
          if [ ! -f ~/.ssh/id_rsa ]; then
            echo "❌ Error: Failed to save private key"
            exit 1
          fi
          
          # Add server to known_hosts with retry logic
          MAX_RETRIES=3
          RETRY_DELAY=5
          
          echo "📝 Attempting to add server to known_hosts..."
          for ((i=1; i<=$MAX_RETRIES; i++)); do
            echo "🔍 Attempt $i/$MAX_RETRIES - Running ssh-keyscan..."
            if ssh-keyscan -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_HOST }} 2> ssh-keyscan-error.log | tee -a ~/.ssh/known_hosts; then
              echo "✅ Successfully added host to known_hosts"
              break
            else
              if [ $i -eq $MAX_RETRIES ]; then
                echo "❌ Failed to add host to known_hosts after $MAX_RETRIES attempts"
                echo "🔍 ssh-keyscan error output:"
                cat ssh-keyscan-error.log || true
                echo "🔍 Current known_hosts content:"
                cat ~/.ssh/known_hosts || true
                echo "🔍 Testing network connectivity..."
                ping -c 3 ${{ secrets.SSH_HOST }} || true
                exit 1
              fi
              echo "⚠️  Failed to add host (attempt $i/$MAX_RETRIES), retrying in ${RETRY_DELAY}s..."
              sleep $RETRY_DELAY
            fi
          done
          
          # Verify known_hosts was updated
          if ! grep -q "${{ secrets.SSH_HOST }}" ~/.ssh/known_hosts 2>/dev/null; then
            echo "❌ Failed to add host to known_hosts after $MAX_RETRIES attempts"
            echo "🔍 Known_hosts content:"
            cat ~/.ssh/known_hosts 2>/dev/null || echo "No known_hosts file found"
            exit 1
          fi
          
          # Test connection with retry logic
          MAX_RETRIES=3
          RETRY_DELAY=10
          
          for ((i=1; i<=$MAX_RETRIES; i++)); do
            echo "\n🔌 Attempt $i/$MAX_RETRIES - Testing SSH connection to ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.SSH_PORT }}"
            
            # SSH command with verbose debug output
            if ssh -v -v -v $SSH_OPTS -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "echo '✅ SSH connection successful!'"; then
              echo "✅ Successfully connected to ${{ secrets.SSH_HOST }}:${{ secrets.SSH_PORT }}"
              exit 0  # Exit with success
            else
              SSH_EXIT_CODE=$?
              echo "⚠️  SSH connection failed with code: $SSH_EXIT_CODE"
              
              if [ $i -eq $MAX_RETRIES ]; then
                echo "❌ Failed to connect after $MAX_RETRIES attempts"
                
                # Additional debug information
                echo "\n🔍 Debug information:"
                echo "- Current directory: $(pwd)"
                echo "- SSH directory contents:"
                ls -la ~/.ssh/
                echo "- Known hosts file:"
                cat ~/.ssh/known_hosts || true
                
                exit 1
              fi
              
              echo "⏳ Retrying in ${RETRY_DELAY}s... ($((MAX_RETRIES-i)) attempts remaining)"
              sleep $RETRY_DELAY
            fi
          done

      - name: Copy files to server
        run: |
          set -e
          echo "📦 Preparing files for deployment..."
          
          # Save Docker image to tar file
          IMAGE_TAR="${{ env.IMAGE_NAME }}.tar"
          echo "💾 Saving Docker image to $IMAGE_TAR..."
          docker save -o $IMAGE_TAR ${{ env.IMAGE_NAME }}:${{ steps.set_tag.outputs.tag }}
          
          # Copy files to server
          echo "📤 Transferring files to server on port ${{ secrets.SSH_PORT }}..."
          scp $SSH_OPTS -P ${{ secrets.SSH_PORT }} \
            $IMAGE_TAR \
            docker-compose.yml \
            docker-swarm.yml \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:~/ || {
              echo "❌ Failed to copy files to server"
              exit 1
            }
          
          echo "✅ Files successfully transferred to server"

      - name: Deploy with Docker Swarm
        env:
          DEPLOY_TAG: ${{ steps.set_tag.outputs.tag }}
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
        run: |
          echo "🚀 Starting deployment process on port ${{ secrets.SSH_PORT }}..."
          
          ssh $SSH_OPTS -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} << 'ENDSSH'
            set -e
            
            echo "🔍 Deployment details:"
            echo "  - Image: $IMAGE_NAME"
            echo "  - Tag: $DEPLOY_TAG"
            echo "  - Timestamp: $(date)"
            
            # Load the Docker image
            echo "\n📥 Loading Docker image..."
            docker load -i ~/$IMAGE_NAME.tar || {
              echo "❌ Failed to load Docker image"
              exit 1
            }
            
            # Set environment variable for docker-compose and docker-swarm
            export IMAGE_TAG=$DEPLOY_TAG
            
            # Deploy with docker-compose (for single node testing)
            echo "\n🐳 Deploying with docker-compose..."
            docker-compose down 2>/dev/null || true
            docker-compose up -d
            
            # Show running containers
            echo "\n📦 Running containers:"
            docker ps --format "table {{.ID}}\t{{.Names}}\t{{.Status}}\t{{.Ports}}"
            
            # Initialize Docker Swarm if not already initialized
            if ! docker node ls &>/dev/null; then
              echo "\n🐝 Initializing Docker Swarm..."
              docker swarm init --advertise-addr 127.0.0.1 || \
                echo "⚠️  Swarm already initialized or error occurred"
            fi
            
            # Deploy with Docker Swarm (5 replicas)
            echo "\n🚀 Deploying with Docker Swarm (5 replicas)..."
            docker stack deploy -c docker-swarm.yml academia-novit
            
            # Wait for services to start
            echo "\n⏳ Waiting for services to stabilize..."
            sleep 15
            
            # Show services and their status
            echo "\n📊 Docker services status:"
            docker service ls
            
            # Show service details
            echo "\n🔍 Service details:"
            docker service ps academia-novit_web --no-trunc || true
            
            # Show network information
            echo "\n🌐 Network information:"
            docker network ls
            
            # Show service logs
            echo "\n📝 Service logs (last 10 lines):"
            docker service logs --tail 10 academia-novit_web 2>&1 || true
            
            # Verify service health
            echo "\n✅ Deployment completed successfully!"
            echo "🔄 Services are being deployed with 5 replicas"
            echo "🔗 API should be available shortly at: http://localhost:80"
          ENDSSH
